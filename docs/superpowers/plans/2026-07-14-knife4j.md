# Knife4j 集成 + 环境隔离 + 启动 Banner 实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 引入 Knife4j，测试环境可访问 `/doc.html` 查看与调试接口，启动控制台打印访问链接；生产环境（profile `prod`）文档资源完全不可访问。

**Architecture:** 依赖 Knife4j 4.5.0（springdoc 2.5.0 之上增强）。用 Spring profile `prod` 做环境隔离：生产经 `application-prod.yml` 禁用 Knife4j/springdoc + Security 层在 prod 不放行文档路径，形成 404/401 双保险；非 prod 文档页 permitAll，`/api/**` 仍 Basic Auth。新增 `OpenApiConfig`（`@Profile("!prod")`）提供 BasicAuth securityScheme；新增 `StartupBanner`（`ApplicationRunner`）打印中文启动面板。

**Tech Stack:** Spring Boot 3.2.7、Knife4j 4.5.0、springdoc-openapi 2.5.0、Spring Security、JUnit 5 + MockMvc + AssertJ。

## Global Constraints

- 远程 MySQL `121.89.92.242:3306/finhub` 可达（`@SpringBootTest` 加载全上下文需连库）；不可达时集成测按 `assumeTrue` 跳过。
- 全程 TDD：每个 task 先写测试契约（RED）-> 最小实现（GREEN）-> 全量 `mvn test` 回归。
- 遵循 dgg-spring-style：构造器注入（`private final` + 构造器或 `@RequiredArgsConstructor`，禁字段 `@Autowired`）、中文 Javadoc/日志、Knife4j 注解（Controller 已具备）。
- 枚举/枚举名、`finhub.encryption.key` 必须 32 字节等既有约束不变（本计划不触碰领域层）。
- commit message 用 gitmoji + 中文（项目历史惯例）。

---

## File Structure

| 文件 | 操作 | 职责 |
|------|------|------|
| `pom.xml` | 修改 | 新增 knife4j 依赖 |
| `src/main/resources/application-prod.yml` | 新增 | 生产配置：禁用 Knife4j + springdoc |
| `src/main/java/com/finhub/infra/config/SecurityConfig.java` | 修改 | profile 感知路由：非 prod 放行文档页 |
| `src/main/java/com/finhub/infra/config/OpenApiConfig.java` | 新增 | `@Profile("!prod")` OpenAPI 元数据 + BasicAuth scheme |
| `src/main/java/com/finhub/infra/config/StartupBanner.java` | 新增 | `ApplicationRunner` 启动面板（纯方法 `buildPanel`） |
| `src/test/java/com/finhub/infra/config/Knife4jDevAccessTest.java` | 新增 | 非 prod：/doc.html 免认证 + /api/** 仍需认证 |
| `src/test/java/com/finhub/infra/config/Knife4jProdDisableTest.java` | 新增 | prod：/v3/api-docs 404（springdoc 禁用） |
| `src/test/java/com/finhub/infra/config/OpenApiConfigTest.java` | 新增 | OpenAPI bean 含 BasicAuth scheme |
| `src/test/java/com/finhub/infra/config/StartupBannerTest.java` | 新增 | buildPanel 内容（dev vs prod）纯单测 |

---

## Task 1: 引入 Knife4j 依赖

**Files:**
- Modify: `pom.xml`
- Test: `mvn test -Dtest=FinHubApplicationTest`（已有，验证引入依赖后上下文仍可加载）

**Interfaces:**
- Produces: `/doc.html`、Knife4j 资源端点（由 starter 自动装配，非 prod 可用）

- [ ] **Step 1: 在 `pom.xml` 新增 Knife4j 依赖**

在 `<dependencies>` 中、`springdoc-openapi-starter-webmvc-ui` 依赖之后新增：

```xml
        <!-- Knife4j（基于 springdoc 增强，提供 /doc.html 文档 UI） -->
        <dependency>
            <groupId>com.github.xiaoymin</groupId>
            <artifactId>knife4j-openapi3-jakarta-spring-boot-starter</artifactId>
            <version>4.5.0</version>
        </dependency>
```

- [ ] **Step 2: 验证依赖解析 + 上下文仍可加载**

Run:
```bash
mvn dependency:get -Dartifact=com.github.xiaoymin:knife4j-openapi3-jakarta-spring-boot-starter:4.5.0 -q
mvn test -Dtest=FinHubApplicationTest
```
Expected: 两条命令均 BUILD SUCCESS（依赖可解析；全上下文含 Knife4j 自动配置仍可启动）。若 `FinHubApplicationTest` 因 springdoc 版本冲突失败，确认 `mvn dependency:tree -Dincludes=org.springdoc:*` 中 springdoc 仍为 2.5.0（项目显式声明优先）。

- [ ] **Step 3: Commit**

```bash
git add pom.xml
git commit -m "🔨 chore(infra): 引入 Knife4j 4.5.0 依赖（springdoc 2.5.0 之上增强）"
```

---

## Task 2: SecurityConfig profile 感知路由（非 prod 放行文档页）

**Files:**
- Modify: `src/main/java/com/finhub/infra/config/SecurityConfig.java`
- Test: `src/test/java/com/finhub/infra/config/Knife4jDevAccessTest.java`

**Interfaces:**
- Consumes: Task 1 的 Knife4j（提供 `/doc.html` 端点）
- Produces: 非 prod 时 `/doc.html` `/swagger-ui/**` `/swagger-resources/**` `/v3/api-docs/**` `/webjars/**` permitAll；prod 时不放行（落到 authenticated）

- [ ] **Step 1: 写失败测试 `Knife4jDevAccessTest`**

```java
package com.finhub.infra.config;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.web.servlet.MockMvc;

import static org.assertj.core.api.Assertions.assertThat;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

/**
 * 非 prod 环境 Knife4j 文档访问契约（默认 profile）。
 *
 * <p>验证：/doc.html 免认证可访问（非 4xx）；/api/** 真实接口仍需 Basic Auth。</p>
 */
@Tag("integration")
@SpringBootTest
@AutoConfigureMockMvc
class Knife4jDevAccessTest {

    @Autowired
    private MockMvc mockMvc;

    @Test
    @DisplayName("非 prod：/doc.html 应免认证可访问（状态码 < 400）")
    void docHtmlShouldBeAccessibleWithoutAuthInDev() throws Exception {
        mockMvc.perform(get("/doc.html"))
                .andExpect(result -> {
                    int status = result.getResponse().getStatus();
                    assertThat(status)
                            .as("/doc.html 在非 prod 应免认证可访问（permitAll），实际状态: " + status)
                            .isLessThan(400);
                });
    }

    @Test
    @DisplayName("非 prod：/api/** 仍需 Basic Auth（未认证返回 401）")
    void apiShouldStillRequireAuthInDev() throws Exception {
        mockMvc.perform(get("/api/transactions/import"))
                .andExpect(status().isUnauthorized());
    }
}
```

- [ ] **Step 2: 运行测试，确认失败（RED）**

Run: `mvn test -Dtest=Knife4jDevAccessTest`
Expected: `docHtmlShouldBeAccessibleWithoutAuthInDev` FAIL（当前所有路由需认证，/doc.html 返回 401，401 >= 400 断言失败）；`apiShouldStillRequireAuthInDev` PASS（既有行为）。失败原因是安全未放行文档页，非编译错误。

- [ ] **Step 3: 改造 `SecurityConfig` 实现 profile 感知路由**

将 `SecurityConfig` 改为（注入 `Environment`，按 `prod` 分支放行文档路径；`username`/`password` 保持既有 `@Value` 字段注入，`userDetailsService`/`passwordEncoder` 不变）：

```java
package com.finhub.infra.config;

import org.springframework.beans.factory.annotation.Value;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.core.env.Environment;
import org.springframework.core.env.Profiles;
import org.springframework.security.config.annotation.web.builders.HttpSecurity;
import org.springframework.security.config.annotation.web.configuration.EnableWebSecurity;
import org.springframework.security.config.http.SessionCreationPolicy;
import org.springframework.security.core.userdetails.User;
import org.springframework.security.core.userdetails.UserDetailsService;
import org.springframework.security.crypto.bcrypt.BCryptPasswordEncoder;
import org.springframework.security.crypto.password.PasswordEncoder;
import org.springframework.security.provisioning.InMemoryUserDetailsManager;
import org.springframework.security.web.SecurityFilterChain;

/**
 * 安全配置：HTTP Basic Auth，用户名/密码通过环境变量注入。
 *
 * <p>文档访问策略：非 prod 放行 Knife4j/springdoc 文档路径（permitAll），
 * 便于测试环境查看与调试；prod 不放行（落到 authenticated），配合
 * application-prod.yml 的 springdoc 禁用形成 404/401 双保险。</p>
 */
@Configuration
@EnableWebSecurity
public class SecurityConfig {

    private final Environment environment;

    public SecurityConfig(Environment environment) {
        this.environment = environment;
    }

    @Value("${finhub.auth.username:admin}")
    private String username;

    @Value("${finhub.auth.password:}")
    private String password;

    /** 非 prod 放行的文档路径（Knife4j + springdoc 资源） */
    private static final String[] DOC_PATHS = {
            "/doc.html",
            "/swagger-ui/**",
            "/swagger-resources/**",
            "/v3/api-docs/**",
            "/webjars/**"
    };

    @Bean
    public SecurityFilterChain securityFilterChain(HttpSecurity http) throws Exception {
        http
            .csrf(csrf -> csrf.disable())
            .sessionManagement(sm -> sm.sessionCreationPolicy(SessionCreationPolicy.STATELESS))
            .authorizeHttpRequests(auth -> {
                auth.requestMatchers("/actuator/health").permitAll();
                if (!isProd()) {
                    auth.requestMatchers(DOC_PATHS).permitAll();
                }
                auth.anyRequest().authenticated();
            })
            .httpBasic(httpBasic -> {});

        return http.build();
    }

    /** 是否处于生产 profile（spring.profiles.active 含 prod） */
    private boolean isProd() {
        return environment.acceptsProfiles(Profiles.of("prod"));
    }

    @Bean
    public UserDetailsService userDetailsService() {
        String effectivePassword = (password == null || password.isEmpty())
            ? "dev-pass"
            : password;
        return new InMemoryUserDetailsManager(
            User.builder()
                .username(username)
                .password(passwordEncoder().encode(effectivePassword))
                .roles("USER")
                .build()
        );
    }

    @Bean
    public PasswordEncoder passwordEncoder() {
        return new BCryptPasswordEncoder();
    }
}
```

- [ ] **Step 4: 运行测试，确认通过（GREEN）**

Run: `mvn test -Dtest=Knife4jDevAccessTest`
Expected: 2 个测试 PASS（/doc.html < 400；/api/** 401）。

- [ ] **Step 5: 回归**

Run: `mvn test`
Expected: 全绿，无回归（含 Day5 的 `IngestionControllerTest` 仍 Basic Auth）。

- [ ] **Step 6: Commit**

```bash
git add src/main/java/com/finhub/infra/config/SecurityConfig.java src/test/java/com/finhub/infra/config/Knife4jDevAccessTest.java
git commit -m "✨ feat(infra): SecurityConfig profile 感知路由，非 prod 放行 Knife4j 文档页"
```

---

## Task 3: application-prod.yml 生产禁用配置

**Files:**
- Create: `src/main/resources/application-prod.yml`
- Test: `src/test/java/com/finhub/infra/config/Knife4jProdDisableTest.java`

**Interfaces:**
- Produces: prod profile 下 `knife4j.production=true`、`springdoc.api-docs.enabled=false`、`springdoc.swagger-ui.enabled=false`

- [ ] **Step 1: 写失败测试 `Knife4jProdDisableTest`**

```java
package com.finhub.infra.config;

import org.junit.jupiter.api.ActiveProfiles;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.web.servlet.MockMvc;

import static org.springframework.security.test.web.servlet.request.SecurityMockMvcRequestPostProcessors.httpBasic;
import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.get;
import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.status;

/**
 * 生产 profile（prod）下 Knife4j/springdoc 禁用契约。
 *
 * <p>验证：/v3/api-docs 在 prod 返回 404（springdoc.api-docs.enabled=false 致端点未注册）。
 * 注意：必须用 Basic Auth 认证请求，否则 Security 先返回 401，无法区分「端点存在(200)」与「端点已禁用(404)」。</p>
 */
@Tag("integration")
@SpringBootTest
@AutoConfigureMockMvc
@ActiveProfiles("prod")
class Knife4jProdDisableTest {

    @Autowired
    private MockMvc mockMvc;

    @Test
    @DisplayName("prod：/v3/api-docs 应返回 404（springdoc 已禁用，认证请求验证端点不存在）")
    void apiDocsShouldBeDisabledInProd() throws Exception {
        // 用 admin/dev-pass 认证绕过 Security 的 401，才能看到端点真实状态
        mockMvc.perform(get("/v3/api-docs").with(httpBasic("admin", "dev-pass")))
                .andExpect(status().isNotFound());
    }

    @Test
    @DisplayName("prod：/api/** 仍需认证（未认证返回 401，安全基线不变）")
    void apiShouldStillRequireAuthInProd() throws Exception {
        mockMvc.perform(get("/api/transactions/import"))
                .andExpect(status().isUnauthorized());
    }
}
```

- [ ] **Step 2: 运行测试，确认失败（RED）**

Run: `mvn test -Dtest=Knife4jProdDisableTest`
Expected: `apiDocsShouldBeDisabledInProd` FAIL（当前无 application-prod.yml，springdoc 仍启用，认证请求 GET /v3/api-docs 返回 200 而非 404）；`apiShouldStillRequireAuthInProd` PASS。失败原因是 prod 配置缺失，非编译错误。

> 说明：此测试必须用 `httpBasic("admin","dev-pass")` 认证。否则在 prod 下 Security 对未认证请求先返回 401，无法分辨端点是否被禁用（404）。RED 时 springdoc 启用 → 认证后 200；GREEN 时 springdoc 禁用 → 认证后 404。

- [ ] **Step 3: 新增 `application-prod.yml`**

```yaml
# 生产环境配置：profile=prod 时加载
# 生产严禁暴露接口文档，双重禁用 Knife4j + springdoc
knife4j:
  production: true              # Knife4j 原生生产模式，屏蔽所有 Knife4j 资源接口
springdoc:
  api-docs:
    enabled: false              # 关闭原生 OpenAPI JSON（/v3/api-docs 返回 404）
  swagger-ui:
    enabled: false              # 关闭 swagger-ui
```

- [ ] **Step 4: 运行测试，确认通过（GREEN）**

Run: `mvn test -Dtest=Knife4jProdDisableTest`
Expected: 2 个测试 PASS（/v3/api-docs 404；/api/** 401）。

- [ ] **Step 5: 回归**

Run: `mvn test`
Expected: 全绿，无回归。

- [ ] **Step 6: Commit**

```bash
git add src/main/resources/application-prod.yml src/test/java/com/finhub/infra/config/Knife4jProdDisableTest.java
git commit -m "✨ feat(infra): application-prod.yml 双重禁用 Knife4j/springdoc（生产文档不可访问）"
```

---

## Task 4: OpenApiConfig（@Profile("!prod")）提供 BasicAuth securityScheme

**Files:**
- Create: `src/main/java/com/finhub/infra/config/OpenApiConfig.java`
- Test: `src/test/java/com/finhub/infra/config/OpenApiConfigTest.java`

**Interfaces:**
- Produces: 非 prod 时 `OpenAPI` bean（含 `info` + `BasicAuth` securityScheme），供 Knife4j「Authorize」配置 Basic Auth 调试 /api/**

- [ ] **Step 1: 写失败测试 `OpenApiConfigTest`**

```java
package com.finhub.infra.config;

import io.swagger.v3.oas.models.OpenAPI;
import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Tag;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.context.ApplicationContext;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * OpenApiConfig 装配契约（默认 profile，非 prod）。
 *
 * <p>验证：OpenAPI bean 存在且含 BasicAuth securityScheme，
 * 供 Knife4j「Authorize」配置 Basic Auth 调试 /api/** 真实接口。</p>
 */
@Tag("integration")
@SpringBootTest
class OpenApiConfigTest {

    @Autowired
    private ApplicationContext applicationContext;

    @Test
    @DisplayName("非 prod：应提供含 BasicAuth securityScheme 的 OpenAPI bean")
    void shouldProvideOpenApiWithBasicAuthScheme() {
        OpenAPI openAPI = applicationContext.getBean(OpenAPI.class);

        assertThat(openAPI).isNotNull();
        assertThat(openAPI.getComponents()).isNotNull();
        assertThat(openAPI.getComponents().getSecuritySchemes())
                .containsKey("BasicAuth");
        assertThat(openAPI.getInfo()).isNotNull();
        assertThat(openAPI.getInfo().getTitle()).isEqualTo("FinHub API");
    }
}
```

- [ ] **Step 2: 运行测试，确认失败（RED）**

Run: `mvn test -Dtest=OpenApiConfigTest`
Expected: FAIL（无 OpenApiConfig 时 `getBean(OpenAPI.class)` 抛 `NoSuchBeanDefinitionException`，或 bean 存在但无 BasicAuth scheme 致断言失败）。失败原因是 OpenAPI 元数据未配置，非编译错误。

- [ ] **Step 3: 新增 `OpenApiConfig`**

```java
package com.finhub.infra.config;

import io.swagger.v3.oas.models.Components;
import io.swagger.v3.oas.models.OpenAPI;
import io.swagger.v3.oas.models.info.Info;
import io.swagger.v3.oas.models.security.SecurityScheme;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.context.annotation.Profile;

/**
 * OpenAPI 文档元数据配置（仅非 prod）。
 *
 * <p>提供标题/描述/版本与 BasicAuth securityScheme，让 Knife4j「Authorize」
 * 可配置 Basic Auth（admin/dev-pass），从而调试 /api/** 真实接口。
 * prod 下不创建此 bean，配合 springdoc 禁用从源头杜绝文档生成。</p>
 */
@Configuration
@Profile("!prod")
public class OpenApiConfig {

    @Bean
    public OpenAPI finHubOpenAPI() {
        return new OpenAPI()
                .info(new Info()
                        .title("FinHub API")
                        .description("个人资金数据治理中台 - 接口文档")
                        .version("0.1.0"))
                .components(new Components()
                        .addSecuritySchemes("BasicAuth",
                                new SecurityScheme()
                                        .type(SecurityScheme.Type.HTTP)
                                        .scheme("basic")));
    }
}
```

- [ ] **Step 4: 运行测试，确认通过（GREEN）**

Run: `mvn test -Dtest=OpenApiConfigTest`
Expected: PASS（OpenAPI bean 含 BasicAuth scheme + title=FinHub API）。

- [ ] **Step 5: 回归**

Run: `mvn test`
Expected: 全绿，无回归。

- [ ] **Step 6: Commit**

```bash
git add src/main/java/com/finhub/infra/config/OpenApiConfig.java src/test/java/com/finhub/infra/config/OpenApiConfigTest.java
git commit -m "✨ feat(infra): OpenApiConfig 提供 BasicAuth scheme（@Profile 非 prod）"
```

---

## Task 5: StartupBanner（ApplicationRunner 启动面板）

**Files:**
- Create: `src/main/java/com/finhub/infra/config/StartupBanner.java`
- Test: `src/test/java/com/finhub/infra/config/StartupBannerTest.java`

**Interfaces:**
- Produces: 纯方法 `String buildPanel(boolean isProd, String appName, String version, int port, String contextPath, String lanIp)`，启动时由 `ApplicationRunner.run` 调用并 `log.info` 打印

- [ ] **Step 1: 写失败测试 `StartupBannerTest`**

```java
package com.finhub.infra.config;

import org.junit.jupiter.api.DisplayName;
import org.junit.jupiter.api.Test;

import static org.assertj.core.api.Assertions.assertThat;

/**
 * StartupBanner 面板内容契约（纯单测，测 buildPanel 字符串构建）。
 *
 * <p>非 prod：含本地+局域网 /doc.html 链接、API 端点、Basic Auth 凭据、应用名。
 * prod：含「Knife4j 已禁用」，不含链接与凭据。</p>
 */
class StartupBannerTest {

    @Test
    @DisplayName("非 prod：面板应含本地/局域网 doc.html 链接、API 端点、凭据、应用名")
    void shouldContainLinksAndCredentialsWhenNotProd() {
        String panel = StartupBanner.buildPanel(
                false, "FinHub", "0.1.0", 8080, "", "192.168.1.100");

        assertThat(panel)
                .contains("FinHub")
                .contains("0.1.0")
                .contains("http://localhost:8080/doc.html")
                .contains("http://192.168.1.100:8080/doc.html")
                .contains("/api/transactions/import")
                .contains("admin/dev-pass");
    }

    @Test
    @DisplayName("非 prod：contextPath 非空时应拼到链接路径前")
    void shouldIncludeContextPathInLinks() {
        String panel = StartupBanner.buildPanel(
                false, "FinHub", "0.1.0", 8080, "/finhub", "192.168.1.100");

        assertThat(panel).contains("http://localhost:8080/finhub/doc.html");
    }

    @Test
    @DisplayName("prod：面板应含「Knife4j 已禁用」，不含链接与凭据")
    void shouldNotContainLinksAndCredentialsWhenProd() {
        String panel = StartupBanner.buildPanel(
                true, "FinHub", "0.1.0", 8080, "", "192.168.1.100");

        assertThat(panel)
                .contains("Knife4j 已禁用")
                .doesNotContain("/doc.html")
                .doesNotContain("admin/dev-pass");
    }

    @Test
    @DisplayName("局域网 IP 为 N/A 时面板应包含 N/A，不报错")
    void shouldHandleLanIpNA() {
        String panel = StartupBanner.buildPanel(
                false, "FinHub", "0.1.0", 8080, "", "N/A");

        assertThat(panel).contains("N/A");
    }
}
```

- [ ] **Step 2: 运行测试，确认失败（RED）**

Run: `mvn test -Dtest=StartupBannerTest`
Expected: 编译失败（`StartupBanner` 类不存在）= RED。确认失败原因是类缺失。

- [ ] **Step 3: 新增 `StartupBanner`**

```java
package com.finhub.infra.config;

import lombok.extern.slf4j.Slf4j;
import org.springframework.beans.factory.annotation.Value;
import org.springframework.boot.ApplicationArguments;
import org.springframework.boot.ApplicationRunner;
import org.springframework.core.env.Environment;
import org.springframework.core.env.Profiles;
import org.springframework.stereotype.Component;

import java.net.InetAddress;
import java.net.UnknownHostException;

/**
 * 启动面板：服务启动后控制台打印中文 ASCII 面板。
 *
 * <p>非 prod：打印本地+局域网 /doc.html 链接、API 端点、Basic Auth 凭据。
 * prod：仅打印一行「Knife4j 已禁用」，不打印链接与凭据。</p>
 *
 * <p>面板字符串构建为纯静态方法 {@link #buildPanel}，便于单测。</p>
 */
@Slf4j
@Component
public class StartupBanner implements ApplicationRunner {

    private final Environment environment;

    @Value("${spring.application.name:FinHub}")
    private String appName;

    @Value("${finhub.version:0.1.0}")
    private String version;

    @Value("${server.port:8080}")
    private int port;

    @Value("${server.servlet.context-path:}")
    private String contextPath;

    public StartupBanner(Environment environment) {
        this.environment = environment;
    }

    @Override
    public void run(ApplicationArguments args) {
        boolean prod = isProd();
        String lanIp = resolveLanIp();
        String panel = buildPanel(prod, appName, version, port, contextPath, lanIp);
        log.info("\n{}", panel);
    }

    /**
     * 构建启动面板字符串（纯函数，无副作用，便于单测）。
     *
     * @param isProd      是否生产环境
     * @param appName     应用名
     * @param version     版本
     * @param port        端口
     * @param contextPath 上下文路径（可为空）
     * @param lanIp       局域网 IP（不可达时传 "N/A"）
     * @return 面板字符串
     */
    public static String buildPanel(boolean isProd, String appName, String version,
                                    int port, String contextPath, String lanIp) {
        String path = (contextPath == null || contextPath.isBlank()) ? "" : contextPath;
        if (isProd) {
            return """
                    ===================================================
                      %s 启动完成（env=prod，Knife4j 已禁用）
                    ===================================================""".formatted(appName);
        }
        String local = "http://localhost:" + port + path + "/doc.html";
        String lan = "http://" + lanIp + ":" + port + path + "/doc.html";
        String api = "http://localhost:" + port + path + "/api/transactions/import";
        return """
                ===================================================
                  %s  v%s
                --------------------------------------------------
                  环境         : 非 prod（Knife4j 可用）
                  API 文档     : %s
                  局域网文档   : %s
                  API 端点     : %s
                  Basic Auth  : admin/dev-pass
                ===================================================""".formatted(
                appName, version, local, lan, api);
    }

    private boolean isProd() {
        return environment.acceptsProfiles(Profiles.of("prod"));
    }

    /** 解析局域网 IP，失败回退 "N/A" */
    private String resolveLanIp() {
        try {
            return InetAddress.getLocalHost().getHostAddress();
        } catch (UnknownHostException e) {
            return "N/A";
        }
    }
}
```

- [ ] **Step 4: 运行测试，确认通过（GREEN）**

Run: `mvn test -Dtest=StartupBannerTest`
Expected: 4 个测试 PASS。

- [ ] **Step 5: 回归**

Run: `mvn test`
Expected: 全绿，无回归。

- [ ] **Step 6: Commit**

```bash
git add src/main/java/com/finhub/infra/config/StartupBanner.java src/test/java/com/finhub/infra/config/StartupBannerTest.java
git commit -m "✨ feat(infra): StartupBanner 启动面板（非 prod 打印 doc.html 链接+凭据）"
```

---

## Task 6: 全量回归 + 手测验证 + README 更新

**Files:**
- Modify: `README.md`

**Interfaces:**
- N/A（验收 + 文档）

- [ ] **Step 1: 全量回归闸门**

Run: `mvn test`
Expected: 全绿，零失败零跳过（integration 因 DB 不可达跳过除外）。核对新增测试：`Knife4jDevAccessTest`(2) + `Knife4jProdDisableTest`(2) + `OpenApiConfigTest`(1) + `StartupBannerTest`(4) = 9 个新测试全绿；Day5 既有 221 测试无回归。

- [ ] **Step 2: 手测应用启动（默认 profile）**

Run: `mvn spring-boot:run`
Expected:
- 控制台打印 Banner 面板，含 `http://localhost:8080/doc.html` + 局域网链接 + `admin/dev-pass`
- 浏览器访问 `http://localhost:8080/doc.html` 免认证打开，文档渲染 `IngestionController` 接口
- Knife4j「Authorize」配 Basic Auth（admin/dev-pass）后，可调试 `POST /api/transactions/import`
- 停止应用（Ctrl+C）

- [ ] **Step 3: 手测生产 profile（可选，验证隔离）**

Run: `mvn spring-boot:run -Dspring-boot.run.profiles=prod`
Expected:
- 控制台仅打印一行「FinHub 启动完成（env=prod，Knife4j 已禁用）」，无链接无凭据
- `curl -i http://localhost:8080/doc.html` 返回非 200（404/401）
- `curl -i http://localhost:8080/v3/api-docs` 返回 404
- 停止应用

- [ ] **Step 4: 更新 README 开发进度**

在 `README.md` 「开发进度」末尾新增一节：

```markdown
### Knife4j 集成 + 环境隔离 + 启动 Banner
- 引入 Knife4j 4.5.0（基于 springdoc 2.5.0 增强，`/doc.html` 文档 UI）
- profile `prod` 双重隔离：`application-prod.yml` 禁用 Knife4j/springdoc + Security 层 prod 不放行文档路径
- 非 prod 文档页 permitAll，`/api/**` 仍 Basic Auth（Knife4j Authorize 配置调试）
- `OpenApiConfig`（`@Profile("!prod")`）提供 BasicAuth securityScheme
- `StartupBanner`（`ApplicationRunner`）启动打印本地+局域网 doc.html 链接 + 凭据，prod 仅一行
```

- [ ] **Step 5: Commit**

```bash
git add README.md
git commit -m "📝 docs(infra): README 补充 Knife4j 集成与环境隔离说明"
```

---

## 验收清单

- [ ] 非 prod 启动后控制台打印 Banner，含本地+局域网 `/doc.html` 链接 + 凭据
- [ ] 非 prod `/doc.html` 免认证可打开，渲染 `IngestionController` 接口
- [ ] 非 prod Knife4j「Authorize」配 Basic Auth 后可调试 `/api/transactions/import`
- [ ] prod 启动仅打印一行，无链接无凭据
- [ ] prod `/doc.html`、`/v3/api-docs` 均非 200
- [ ] `mvn test` 全绿，无回归（新增 9 测试 + 既有 221 测试）
