/**
 * AI 辅助上下文（ai）— 支撑域
 *
 * <h3>职责边界</h3>
 * <p>通过 ACL 向其他上下文提供 NL→SQL 翻译、分类兜底、异常解释等 AI 能力。
 * 自身不持有业务数据，只做"翻译+建议"，不做"决策"。</p>
 *
 * <h3>边界规则</h3>
 * <ul>
 *   <li>AI 输出必须经过 ACL 校验（如 AST 白名单），不通过则丢弃</li>
 *   <li>核心域的 Transaction 聚合根根本不知道"自然语言"是什么</li>
 *   <li>失败时降级，不阻断主流程</li>
 * </ul>
 */
package com.finhub.ai;